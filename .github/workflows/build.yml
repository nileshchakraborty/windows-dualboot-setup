name: Build & Test DualBoot Launchers

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build and Test Executables
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Build Solution (Release)
        run: dotnet build RestartToBazzite.sln -c Release

      - name: Run Unit Tests
        run: dotnet test RestartToBazzite.sln -c Release --verbosity normal --no-build

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: DualBoot-binaries
          path: |
            src/**/bin/Release/**/RestartTo*.exe
            src/**/*.ico

      - name: Release Assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            src/RestartToBazzite/bin/Release/net48/RestartToBazzite.exe
            src/RestartToSteamOS/bin/Release/net48/RestartToSteamOS.exe
            src/RestartToBazzite/RestartToBazzite.ico
            src/RestartToSteamOS/RestartToSteamOS.ico
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
